---
title: Query Documents
---

import QueryTemplate, {
  toc as queryToc,
} from "@site/docs/templates/_querying-language-reference.template.mdx";
import createCollectionSource from "!!raw-loader!../_snippets/query/_createCollection";
import readOneSource from "!!raw-loader!../_snippets/query/_readOne";
import readManySource from "!!raw-loader!../_snippets/query/_readMany";
import multipleFieldsSource from "!!raw-loader!../_snippets/query/_multipleFields";
import readFieldsSource from "!!raw-loader!../_snippets/query/_readFields";
import rangeReadSource from "!!raw-loader!../_snippets/query/_rangeRead";
import metaFilterSource from "!!raw-loader!../_snippets/query/_metadataFilter";
import logicalFiltersSource from "!!raw-loader!../_snippets/query/_logicalFilters";
import nestedFieldsSources from "!!raw-loader!../_snippets/query/_nestedFields";
import readAllSource from "!!raw-loader!../_snippets/query/_readAll";
import readUsingCollationSource from "!!raw-loader!../_snippets/query/_readUsingCollation";
import sortResultsSource from "!!raw-loader!../_snippets/query/_sortResults";
import paginationSource from "!!raw-loader!../_snippets/query/_pagination";

export const filterSyntax = {
  eq: "$eq",
  lt: "$lt",
  lte: "$lte",
  gt: "$gt",
  gte: "$gte",
  or: "$or",
  and: "$and",
};

export const sortDirectionSyntax = {
  ASC: "$asc",
  DESC: "$desc",
};

export const paginationOptions = {
  limit: "new FindQueryOptions(limit?: number)",
  skip: "new FindQueryOptions(limit?: number, skip: number)",
};

<QueryTemplate
  codeLang="typescript"
  filters={filterSyntax}
  createCollection={createCollectionSource}
  readOne={readOneSource}
  readMany={readManySource}
  multipleFields={multipleFieldsSource}
  readFields={readFieldsSource}
  rangeRead={rangeReadSource}
  metadataFilter={metaFilterSource}
  logicalFilters={logicalFiltersSource}
  nestedFields={nestedFieldsSources}
  readAll={readAllSource}
  readUsingCollation={readUsingCollationSource}
  sortDirection={sortDirectionSyntax}
  sortResults={sortResultsSource}
  paginationOptions={paginationOptions}
  paginationExample={paginationSource}
/>

export const toc = [...queryToc];
