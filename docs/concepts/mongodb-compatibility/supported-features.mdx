# Supported Features

import MongoDBFeatures, {
  toc as featuresToc,
} from "@site/docs/includes/_mongodb_features.mdx";

Tigris MongoDB compatibility enables transparent compatibility with native
MongoDB client SDKs, drivers, and tools. Tigris provides its own SDK as well
which is a feature-rich alternative to MongoDB client SDKs.

<MongoDBFeatures />

export const toc = [...featuresToc];
